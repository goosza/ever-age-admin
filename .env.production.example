# Production Environment Variables
#
# Copy this file to .env.production if you need to override the default.
# In production the admin panel is served behind its own nginx, which proxies
# /api/* to the backend on the same origin — so a relative path is correct
# and no domain-specific value is needed here.

VITE_API_URL=/api
